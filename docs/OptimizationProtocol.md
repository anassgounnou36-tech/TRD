# Optimization Protocol

- Do not optimize by maximizing one backtest metric only
- Prefer robust parameter neighborhoods over single-point peaks
- Validate across different date ranges and conditions
- Keep the core strategy logic unchanged (regime + breakout + ATR exits + risk limits)
- Use walk-forward style checks before considering VPS deployment
